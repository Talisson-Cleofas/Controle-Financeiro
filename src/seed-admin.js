import 'dotenv/config';import mongoose from 'mongoose';import bcrypt from 'bcryptjs';import User from './models/User.js';await mongoose.connect(process.env.MONGODB_URI);const email=process.env.ADMIN_EMAIL;const password=process.env.ADMIN_PASSWORD;if(!email||!password)throw new Error('Configure ADMIN_EMAIL e ADMIN_PASSWORD');await User.findOneAndUpdate({email:email.toLowerCase()},{$set:{name:'Administrador',email:email.toLowerCase(),passwordHash:await bcrypt.hash(password,12),role:'admin',status:'active',plan:'lifetime'}},{upsert:true});console.log('Administrador criado/atualizado:',email);await mongoose.disconnect();
